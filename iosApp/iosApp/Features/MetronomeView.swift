import SwiftUI
import Shared

struct MetronomeView: View {
    var body: some View {
        VStack {
            Text("MetronomeView")
        }
    }
}

struct MetronomeView_Previews: PreviewProvider {
    static var previews: some View {
        MetronomeView()
    }
}
