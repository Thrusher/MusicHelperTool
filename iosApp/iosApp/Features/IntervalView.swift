import SwiftUI
import Shared

struct IntervalView: View {
    var body: some View {
        VStack {
            Text("IntervalView")
        }
    }
}

struct IntervalView_Previews: PreviewProvider {
    static var previews: some View {
        IntervalView()
    }
}
